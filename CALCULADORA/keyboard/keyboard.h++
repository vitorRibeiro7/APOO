#include "iostream"

class keyboard
{
private:
    /* data */
public:
    keyboard(/* args */);

    void findKey(char x);
    void recieveDigit(int x);
    void setCpu(/*cpu8*/);

    ~keyboard();
};
